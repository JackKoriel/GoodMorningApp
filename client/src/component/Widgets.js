import React from "react";
import styled from "styled-components";

const Widgets = () => {
  return <Master>This is the widgets area</Master>;
};

const Master = styled.div`
  flex: 0.25;
`;

export default Widgets;
